import 'package:flutter/material.dart';

class RecentSeriesWidget extends StatelessWidget {
  const RecentSeriesWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder(

    );
  }
}
